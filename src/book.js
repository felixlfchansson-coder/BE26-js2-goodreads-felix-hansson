export class Book {
  constructor(title, author, isRead, rating, expectationRating) {
    this.title = title;
    this.author = author;
    this.isRead = isRead;
    this.rating = rating;
    this.expectationRating = expectationRating;
  }
}